.gradle/
build/
!gradle/wrapper/gradle-wrapper.jar
!gradle/wrapper/gradle-wrapper.properties
.tmp-java/

*.iml
.idea/
out/
.vscode/

*.log

# frontend/ build output — vite writes it straight into src/main/resources/static/ (see
# frontend/vite.config.ts) so Spring Boot serves it without a copy step, but it's a build
# artifact, not source: regenerated by `npm run build` in the Dockerfile/CI, not committed.
frontend/node_modules/
src/main/resources/static/
frontend/tsconfig.tsbuildinfo
