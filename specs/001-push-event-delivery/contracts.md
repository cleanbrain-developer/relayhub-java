# Spec 001: Contracts

Not yet defined. This file will hold the concrete request/response contracts (OpenAPI-described) for:

- Source / Source Event / Target / Subscription registration APIs
- The generated Ingress endpoint shape
- The Canonical Event and Delivery Attempt query APIs

To be filled in during Phase 1 implementation planning, per `docs/architecture/system-design.md`'s registration model and canonical event schema. Do not invent field names here ahead of implementation — derive them from `system-design.md` and `spec.md`.
